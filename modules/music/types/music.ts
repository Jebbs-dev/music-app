export type MusicType = {
  id: string;
  title: string;
  artist: string;
  artwork: string;
  url: string;
  // duration?: number; // Optional duration field
  // isFavorite?: boolean; // Optional favorite status
  // isPlaying?: boolean; // Optional playing status
  // isPaused?: boolean; // Optional paused status
}